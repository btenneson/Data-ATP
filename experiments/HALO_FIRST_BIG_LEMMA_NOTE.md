# Halo(0) first big lemma experiment

Candidate milestone for the Halo(0)≈R program:

> Prove an injection from R into halo(0), e.g. in an ultrapower presentation by mapping r to the class of the sequence r/(n+1), and verify that the image is infinitesimal and the map is injective.

This note is only a planning marker. It does not claim a formal theorem statement, benchmark encoding, or proof until those are inspected and fixed against the repository's actual Halo formalization.
