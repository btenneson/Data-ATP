# NOTALD Massive Tied-Ocean R01

This is the first scored implementation-validation campaign authorized after the proposal freeze candidate.

Frozen choices: 15 horizons from 10 through 750,000; three hidden seed families per horizon; 30-second hard wall-clock budget and 4,096 MiB process memory cap; all smaller-L sealed siblings plus independently verified certificates offered to learning systems; no held-out certificate, answer label, shortest route, search trace, or shared-bank trace exposed.

Only NOTALD receives the negated conjecture. Its R-side double-negation normalization to the native theorem costs zero Ocean inferences. `BOUNDED_UNKNOWN` is never a logical settlement.

`Depths-F_Ocean_Control_1.0` and `NOTALD_Ocean_Implementation_Reference_0.1` are benchmark-specific implementation-validation references, not claims that production general-purpose versions exist. The Data-ATP and DATA 2.0.1 adapters are likewise explicitly Ocean reference implementations.

Professional ATP theorem claims are accepted only if the SZS proof/refutation block exposes named input Ocean edge axioms whose induced subgraph independently contains a source-to-target path. A theorem status line without that certificate is an `AUDIT_FAILURE` for R01 scoring.

The R01 generator is linearly scaled. It plants a required L-edge backbone, adds only longer reconnecting detours and non-reconnecting dead spurs, relabels nodes, shuffles presentation, strips answer-bearing metadata, and then independently BFS-verifies the sealed graph. The BFS result is authoritative.
