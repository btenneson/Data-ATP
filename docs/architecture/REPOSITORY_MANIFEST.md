# Repository preservation manifest

This private repository is intended to be the durable source of truth for the current Data-ATP concept.

## Preserved architecture documents

The editable repository snapshot is in `docs/archive/PHASE0_ARCHITECTURE_SNAPSHOT.md`. The page-faithful Phase 0 and Moriarty PDFs remain preserved in the user's private file library; the Moriarty LaTeX source is also stored in this repository.

## New command-autonomy material

- `docs/architecture/ADR-0001-accountable-autonomy.md`
- `docs/architecture/IMPLEMENTATION_PLAN.md`
- executable scaffold in `src/data_atp/`
- regression tests in `tests/`

## New budget-coverage research material

- LaTeX source and figure-generation program in `docs/research/`
- the compiled PDF is preserved as a conversation deliverable and in the user's private file library

## Preservation policy

Future architecture changes should update this manifest, preserve the previous version or commit, include source where available, record hashes for external formal libraries and verifiers, and never replace a failed experiment with only a summary.
